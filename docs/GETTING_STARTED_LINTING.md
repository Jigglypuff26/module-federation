# 🚀 Быстрый старт с ESLint и Prettier

Добро пожаловать! Это краткое руководство поможет вам за 5 минут настроить линтинг и начать работать с проектом.

## ⏱️ 5 минут до старта

### Шаг 1: Установка (2 минуты)

```bash
# Клонируйте проект (если еще не сделали)
git clone <repository-url>
cd module-federation

# Установите все зависимости
npm install
npm run install:all
```

### Шаг 2: VS Code расширения (1 минута)

При открытии проекта в VS Code появится уведомление:

```
This workspace has extension recommendations.
Would you like to install them?
```

Нажмите **"Install All"**.

Или установите вручную:

1. Откройте Extensions (`Ctrl+Shift+X`)
2. Найдите и установите:
   - **ESLint** (dbaeumer.vscode-eslint)
   - **Prettier** (esbenp.prettier-vscode)
   - **Volar** (Vue.volar) - для Vue

### Шаг 3: Перезагрузка (30 секунд)

```
Ctrl+Shift+P → Developer: Reload Window
```

### Шаг 4: Проверка (1 минута)

Откройте любой `.js`, `.jsx`, `.ts`, `.tsx` или `.vue` файл:

1. Добавьте строку:

   ```javascript
   const test = 'hello';
   ```

2. Сохраните файл (`Ctrl+S`)

3. Результат должен быть:
   ```javascript
   const test = 'hello';
   ```

**🎉 Готово!** Если форматирование сработало - все настроено правильно.

## ✅ Что дальше?

### Основные команды

```bash
# Проверить код перед коммитом
npm run lint

# Исправить автоматически
npm run lint:fix

# Отформатировать все файлы
npm run format
```

### Работа в редакторе

- `Ctrl+S` - сохранить и автоматически отформатировать
- `Shift+Alt+F` - форматировать документ
- `Ctrl+Shift+P` → `ESLint: Fix all auto-fixable Problems`

### Рекомендуемый workflow

1. **Пишите код** как обычно
2. **Сохраняйте файл** (`Ctrl+S`) - автоформатирование
3. **Перед коммитом** запустите `npm run lint`
4. **Если есть ошибки** - `npm run lint:fix`
5. **Коммитьте** чистый код

## 📋 Основные правила (кратко)

### ✅ DO (делайте так)

```javascript
// Одинарные кавычки
const name = 'John';

// Стрелочные функции для компонентов
const Button = () => <button>Click</button>;

// Точка с запятой
const sum = (a, b) => a + b;

// camelCase для переменных
const userName = 'John';

// PascalCase для компонентов
const UserProfile = () => {};
```

### ❌ DON'T (не делайте так)

```javascript
// Двойные кавычки (кроме JSX)
const name = 'John';

// function для компонентов
function Button() {
  return <button>Click</button>;
}

// console.log (используйте console.warn/error)
console.log('debug');

// snake_case
const user_name = 'John';
```

## 🎓 Документация

### Для быстрого старта:

- [**LINTING_CHEATSHEET.md**](LINTING_CHEATSHEET.md) - Шпаргалка команд

### Для углубленного изучения:

- [**LINTING.md**](LINTING.md) - Полная документация
- [**ESLINT_RULES_EXAMPLES.md**](ESLINT_RULES_EXAMPLES.md) - Примеры кода

### Для настройки:

- [**LINTING.md**](LINTING.md) - Полная документация с настройками

## 🆘 Проблемы?

### ESLint не работает в VS Code?

```bash
# 1. Проверьте установку расширения
code --install-extension dbaeumer.vscode-eslint

# 2. Перезапустите ESLint
Ctrl+Shift+P → ESLint: Restart ESLint Server

# 3. Перезагрузите VS Code
Ctrl+Shift+P → Developer: Reload Window
```

### Prettier не форматирует?

```bash
# 1. Проверьте установку
code --install-extension esbenp.prettier-vscode

# 2. Проверьте настройки (Ctrl+,)
Поиск: "format on save" → должно быть включено

# 3. Установите Prettier по умолчанию
Ctrl+Shift+P → Format Document With...
→ Configure Default Formatter...
→ Prettier
```

### Конфликты форматирования?

```bash
# Очистите кеш
rm -rf .eslintcache
rm -rf node_modules/.cache

# Переустановите
npm install
```

## 💡 Советы

1. **Не игнорируйте ошибки ESLint** - они помогают писать качественный код
2. **Используйте автоисправление** - `npm run lint:fix` исправляет большинство проблем
3. **Читайте сообщения ESLint** - они объясняют, что не так и как исправить
4. **Сохраняйте чаще** - автоформатирование работает при сохранении
5. **Изучайте примеры** - откройте [ESLINT_RULES_EXAMPLES.md](ESLINT_RULES_EXAMPLES.md)

## 🔥 Pro Tips

### Tip 1: Форматировать только измененные файлы

```bash
# Форматировать измененные файлы
git diff --name-only | grep -E '\.(js|jsx|ts|tsx|vue)$' | xargs npx prettier --write

# Проверить только измененные
git diff --name-only | grep -E '\.(js|jsx|ts|tsx|vue)$' | xargs npx eslint
```

### Tip 2: Pre-commit hooks (опционально)

Автоматическая проверка перед каждым коммитом:

```bash
npm i -D husky lint-staged
npx husky install
npx husky add .husky/pre-commit "npx lint-staged"
```

### Tip 3: Отключить правило временно

```javascript
// Для одной строки
console.log('test'); // eslint-disable-line no-console

// Для блока
/* eslint-disable no-console */
console.log('line 1');
console.log('line 2');
/* eslint-enable no-console */
```

### Tip 4: Проверить конкретный файл

```bash
# ESLint
npx eslint src/App.jsx

# Prettier
npx prettier --check src/App.jsx
npx prettier --write src/App.jsx
```

## 🎯 Финальная проверка

Убедитесь, что все работает:

```bash
# 1. Проверка кода
npm run lint
# ✅ Должно пройти без ошибок (или показать существующие)

# 2. Исправление
npm run lint:fix
# ✅ Должно исправить автоматически исправимые ошибки

# 3. Форматирование
npm run format
# ✅ Должно отформатировать все файлы
```

## 📞 Нужна помощь?

1. Проверьте [LINTING_CHEATSHEET.md](LINTING_CHEATSHEET.md) - шпаргалка команд
2. Прочитайте [Troubleshooting в LINTING.md](LINTING.md#troubleshooting)
3. Посмотрите примеры в [ESLINT_RULES_EXAMPLES.md](ESLINT_RULES_EXAMPLES.md)

---

**Теперь вы готовы!** 🚀

Начните кодить, и линтер будет автоматически следить за качеством вашего кода.
