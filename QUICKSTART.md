# ⚡ Быстрый Старт

## 🎯 За 3 шага к запуску

### 1️⃣ Установка зависимостей

```bash
npm run install:all
```

### 2️⃣ Запуск всех приложений

```bash
npm run start:all
```

### 3️⃣ Открыть в браузере

```
http://localhost:3000
```

## 📝 Детальная инструкция

### Если хотите запустить приложения отдельно:

**Терминал 1 - React Remote (должен запуститься первым):**
```bash
cd react-remote
npm install
npm start
```

**Терминал 2 - Vue Remote:**
```bash
cd vue-remote
npm install
npm start
```

**Терминал 3 - Angular Remote:**
```bash
cd angular-remote
npm install
npm start
```

**Терминал 4 - Host (запускается последним):**
```bash
cd host
npm install
npm start
```

## ✅ Проверка

Если все работает правильно, вы увидите:

- ✅ Host на http://localhost:3000
- ✅ React Remote на http://localhost:3001
- ✅ Vue Remote на http://localhost:3002
- ✅ Angular Remote на http://localhost:3003

## 🎨 Что делать дальше?

1. Откройте http://localhost:3000
2. Используйте навигацию для переключения между приложениями
3. Наслаждайтесь работой Module Federation!

## ⚠️ Возможные проблемы

### Порты заняты?
Проверьте, что порты 3000-3003 свободны:

**Windows:**
```powershell
netstat -ano | findstr :3000
netstat -ano | findstr :3001
netstat -ano | findstr :3002
netstat -ano | findstr :3003
```

**Linux/Mac:**
```bash
lsof -i :3000
lsof -i :3001
lsof -i :3002
lsof -i :3003
```

### Ошибка загрузки remote модулей?
Убедитесь, что ВСЕ удаленные приложения запущены перед открытием Host.

## 📚 Дополнительно

Подробная документация в [README.md](README.md)
